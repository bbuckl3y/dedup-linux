"""
Find duplicate graphics files.
Graphics files have certain finename extensions, e.g. .jpg or .png
Later, they may be linked so there is only one copy.
"""
import os
import argparse

def main()
    """
    DeDup main routine.
    """
    os.chdir("/media/samba/s4") # use argparse later ...
    
    return

if __name__ == "__main__"
    main()
